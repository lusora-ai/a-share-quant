# Base System Configuration
project_name: "ashare-quant"
version: "1.0.0"
random_seed: 42

paths:
  data_dir: "data"
  raw_dir: "data/raw"
  processed_dir: "data/processed"
  cache_dir: "data/cache"
  experiments_dir: "experiments"
  reports_dir: "reports"
  db_path: "data/quant_lab.duckdb"

logging:
  level: "INFO"
  console_output: true
  file_output: true
  log_file: "reports/quant_lab.log"
